{
  "name": "rc-transforms",
  "version": "1.0.0",
  "description": "A transformation layer that sits between orm and json responses",
  "repository": "https://github.com/Thanatad/rc-transforms-ts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w",
    "test": "npx jest",
    "test:coverage": "npx jest --coverage --coverageDirectory='coverage'",
    "test:coveralls": "npx jest --coverage && coveralls < coverage/lcov.info"
  },
  "keywords": [
    "laravel resources api",
    "resources",
    "laravel collection",
    "transformer"
  ],
  "author": "Thanatad B.",
  "license": "MIT",
  "dependencies": {
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "coveralls": "^3.1.1",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.7"
  }
}
